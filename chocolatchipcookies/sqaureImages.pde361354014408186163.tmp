void sqaureImages 

() {
   if ( key=='A' || key=='a' ){
    filter(GRAY);
    }
       if ( key=='C' || key=='c' ){
    filter(POSTERIZE, 5);
    }
      if ( key=='B' || key=='b' ) {
    filter(INVERT);
      }
 
  if ( key=='D' || key=='d' ) {
    filter(BLUR);
      }
 
  image(img1, leftSquareX, leftSquareY, leftSquareWidth, leftSquareHeight);
  image(img2, topLeftX, topLeftY, topLeftWidth, topLeftHeight);
  image(img8, topSquareX, topSquareY, topSquareWidth, topSquareHeight);
  image(img6, bottomRightX, bottomRightY, bottomRightWidth, bottomRightHeight );
  image(img4, bottomSquareX, bottomSquareY, bottomSquareWidth, bottomSquareHeight);
  image(img9, rightSquareX, rightSquareY, rightSquareWidth, rightSquareHeight);
  image(img5, bottomLeftX, bottomLeftY, bottomLeftWidth, bottomLeftHeight);
  image(img7, topRightX, topRightY, topRightWidth, topRightHeight);
}
